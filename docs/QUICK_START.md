# 🚀 دليل البدء السريع

## 📦 التثبيت والإعداد

### 1. تثبيت المكتبات المطلوبة
```bash
cd book_distribution_system
flutter pub get
```

### 2. إعداد الصلاحيات للأندرويد

افتح الملف: `android/app/src/main/AndroidManifest.xml`

أضف الصلاحيات التالية داخل `<manifest>`:

```xml
<!-- صلاحيات الإنترنت -->
<uses-permission android:name="android.permission.INTERNET" />

<!-- صلاحيات GPS -->
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

<!-- صلاحيات الكاميرا -->
<uses-permission android:name="android.permission.CAMERA" />

<!-- صلاحيات الملفات -->
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
```

### 3. تشغيل التطبيق

```bash
# تشغيل على Android
flutter run

# أو تشغيل على جهاز محدد
flutter run -d <device_id>

# لعرض الأجهزة المتاحة
flutter devices
```

## 🔐 حسابات الاختبار

### للمندوبين (Drivers):
```
Username: driver
Password: driver123
Role: ministry_driver
```

### لموظفي المدارس:
```
Username: school
Password: school123
Role: school_staff
```

## 📱 كيفية استخدام التطبيق

### للمندوبين:

1. **تسجيل الدخول**
   - استخدم اسم المستخدم: `driver` وكلمة السر: `driver123`

2. **عرض الشحنات النشطة**
   - التبويب الأول يعرض جميع الشحنات المسندة إليك

3. **بدء التوصيل**
   - انقر على الشحنة
   - اضغط "بدء التوصيل"
   - سيتم تسجيل موقعك تلقائياً

4. **التقاط صورة الإثبات**
   - في شاشة تفاصيل الشحنة
   - اضغط "التقاط صورة الإثبات"
   - التقط صورة للشحنة

5. **إكمال التوصيل**
   - أدخل اسم المستلم
   - أضف ملاحظات (اختياري)
   - اضغط "إكمال التوصيل"

6. **عرض الإحصائيات**
   - التبويب الثالث يعرض إحصائيات أدائك

### لموظفي المدارس:

1. **تسجيل الدخول**
   - استخدم: `school` / `school123`

2. **عرض الشحنات الواردة**
   - التبويب الأول يعرض الشحنات الواردة لمدرستك

3. **استلام بـ QR Code (المفضل)** ⭐
   - اضغط زر "استلام شحنة" (أسفل الشاشة)
   - أدخل رمز QR يدوياً أو امسحه
   - أدخل اسمك
   - أضف ملاحظات (اختياري)
   - اضغط "تأكيد الاستلام"

4. **استلام يدوي** (بديل)
   - اضغط "استلام يدوي" على الشحنة
   - أدخل اسم المستلم
   - اختر حالة الشحنة (جيدة/تالفة/جزئية)
   - أضف ملاحظات
   - اضغط "تأكيد"

5. **عرض السجل**
   - التبويب الثاني يعرض جميع الشحنات المستلمة

## 🔧 إعدادات الـ API

إذا كنت تريد تغيير عنوان الخادم:

افتح: `lib/utils/constants.dart`

```dart
class AppConfig {
  static const String apiBaseUrl = 'http://45.77.65.134';  // غير هذا
  // ...
}
```

## ⚠️ حل المشاكل الشائعة

### المشكلة: لا يعمل GPS
**الحل**:
1. تأكد من تفعيل GPS في الجهاز
2. امنح التطبيق صلاحيات الموقع
3. أعد تشغيل التطبيق

### المشكلة: لا تعمل الكاميرا
**الحل**:
1. امنح التطبيق صلاحيات الكاميرا
2. تأكد من عدم استخدام الكاميرا من تطبيق آخر

### المشكلة: "Connection refused" أو "Network error"
**الحل**:
1. تأكد من اتصالك بالإنترنت
2. تحقق من عنوان API في `constants.dart`
3. تأكد من تشغيل الخادم

### المشكلة: "Invalid token" أو "401 Unauthorized"
**الحل**:
1. سجل خروج ثم سجل دخول مرة أخرى
2. Token قد يكون منتهي الصلاحية

### المشكلة: QR Code "منتهي الصلاحية"
**الحل**:
- QR Code صالح لمدة 72 ساعة فقط
- اطلب QR جديد من النظام

## 📊 حالات الشحنات

| الحالة | الوصف | ماذا تعني |
|--------|-------|-----------|
| `قيد الانتظار` | pending | لم يتم إسنادها لمندوب بعد |
| `تم الإسناد` | assigned | مسندة للمندوب ولم يبدأ التوصيل |
| `خارج للتوصيل` | out_for_delivery | المندوب بدأ عملية التوصيل |
| `تم التسليم` | delivered | تم تسليم الشحنة للمدرسة |
| `مؤكد` | confirmed | تأكيد نهائي من النظام |
| `ملغي` | canceled | تم إلغاء الشحنة |

## 🎯 نصائح للاستخدام الأمثل

### للمناديب:
✅ دائماً اضغط "بدء التوصيل" عند المغادرة  
✅ التقط صورة واضحة للشحنة عند الوصول  
✅ تأكد من إدخال اسم المستلم بشكل صحيح  
✅ راجع إحصائياتك بانتظام  

### للمدارس:
✅ استخدم QR Code للاستلام (أسرع وأدق)  
✅ تحقق من تفاصيل الشحنة قبل الاستلام  
✅ سجل أي مشاكل في ملاحظات الاستلام  
✅ راجع السجل للتأكد من استلام جميع الشحنات  

## 📞 الدعم والمساعدة

للمزيد من التفاصيل راجع:
- `IMPLEMENTATION_SUMMARY.md` - ملخص التنفيذ
- `API_REFERENCE.md` - دليل API الكامل
- الدليل الأصلي المقدم

---

**نسخة التطبيق**: 1.0.0  
**آخر تحديث**: 23 ديسمبر 2024  
**الحالة**: ✅ جاهز للاستخدام
