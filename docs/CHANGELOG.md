# قائمة التغييرات - Changelog

## النسخة 2.0.0 - تكامل Backend (30 نوفمبر 2025)

### ✨ ميزات جديدة

#### API Integration
- إضافة دعم كامل لـ JWT Authentication (access + refresh tokens)
- إضافة تحديث تلقائي للتوكن عند انتهاء الصلاحية
- إضافة معالجة أخطاء 401 تلقائياً
- إضافة دوال مساعدة: `get()`, `post()`, `put()`, `patch()`, `delete()`

#### Models
- إضافة `SchoolRequest` model متوافق مع Backend
- إضافة `SchoolRequestItem` model
- إضافة `Shipment` model للشحنات
- إضافة `ShipmentBook` model

#### Services
- تحديث `AuthService` للتكامل مع `/api/auth/login/` و `/api/auth/refresh/`
- تحديث `OrderService` لاستخدام `/api/school-requests/`
- إضافة `fetchSchoolRequests(schoolId)`
- إضافة `createSchoolRequest(request)`
- إضافة `updateRequestStatus(requestId, status)`

### 🔧 إصلاحات

#### API URLs
- تغيير من `http://localhost:8000` إلى `http://10.0.2.2:8000` للـ Android Emulator
- إضافة تعليقات للاستخدام على الأجهزة الحقيقية

#### Endpoints
- إصلاح endpoint تسجيل الدخول من `/api/auth/login` إلى `/api/auth/login/`
- إصلاح endpoint الطلبات من `/api/orders` إلى `/api/school-requests/`
- إضافة دعم pagination للاستجابات

#### Error Handling
- تحسين معالجة الأخطاء في جميع الخدمات
- إضافة fallback محلي للاختبار بدون Backend
- إضافة try-catch في جميع طلبات API

### 📚 وثائق

- إضافة `INTEGRATION_GUIDE.md` - دليل التكامل الشامل
- إضافة `EXAMPLES.md` - أمثلة كود جاهزة
- إضافة `COMPLETION_REPORT.md` - تقرير الإنجاز
- إضافة `README_AR.md` - دليل سريع بالعربية

### 🔄 تحسينات

- تحسين أداء طلبات API
- إضافة timeout للطلبات (10 ثواني)
- تحسين تحويل JSON للنماذج
- إضافة دعم pagination
- تحسين error messages

### ⚠️ Breaking Changes

- تغيير `Order` model - يُنصح باستخدام `SchoolRequest` للتطبيقات الجديدة
- تغيير API endpoints - يجب استخدام الـ endpoints الجديدة
- تغيير Token format - يستخدم الآن access + refresh بدلاً من token واحد

### 🔜 قادم قريباً

- حفظ التوكنات بشكل دائم مع `flutter_secure_storage`
- دعم Push Notifications
- دعم QR Code للمندوبين
- دعم Offline Mode
- دعم الصور

---

## النسخة 1.0.0 - الإصدار الأولي

### ميزات
- واجهة تسجيل دخول
- dashboard للمدارس
- dashboard للمندوبين
- إنشاء طلبات محلياً
- عرض الطلبات
- نظام الإشعارات البسيط

---

## ترقية من 1.0.0 إلى 2.0.0

### الخطوات المطلوبة:

1. **تحديث الاعتماديات:**
```bash
flutter pub get
```

2. **تحديث Constants:**
```dart
// في lib/utils/constants.dart
static const String apiBaseUrl = 'http://10.0.2.2:8000'; // للـ emulator
```

3. **تحديث استدعاءات API:**
```dart
// قديم
orderService.fetchOrders();

// جديد
orderService.fetchSchoolRequests(schoolId);
```

4. **تحديث Models:**
```dart
// قديم
Order order = ...;

// جديد (للتطبيقات الجديدة)
SchoolRequest request = ...;

// أو استمر باستخدام Order (ما زال مدعوماً)
```

---

## ملاحظات الترقية

- ✅ التوافقية محفوظة - التطبيقات القديمة تعمل
- ✅ يُنصح بالانتقال تدريجياً للـ models الجديدة
- ✅ جميع الميزات القديمة ما زالت تعمل
- ⚠️ Backend يجب أن يكون مشغلاً للميزات الجديدة

---

**ملاحظة:** هذه النسخة تتطلب Backend Django مع الـ endpoints المحددة في `INTEGRATION_GUIDE.md`
