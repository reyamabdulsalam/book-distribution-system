# تعليمات تشغيل تطبيق Flutter 📱

## الطريقة 1: من Windows PowerShell (موصى بها)

1. افتح PowerShell على Windows
2. شغل الأمر التالي:
```powershell
cd "\\wsl$\Ubuntu\home\reyam\ketabi\mobile\book_distribution_system"
flutter run
```

## الطريقة 2: استخدام السكريبت الجاهز

```powershell
cd "\\wsl$\Ubuntu\home\reyam\ketabi\mobile\book_distribution_system"
.\run-flutter.ps1
```

## الطريقة 3: Android Studio

1. افتح Android Studio
2. افتح المشروع من: `\\wsl$\Ubuntu\home\reyam\ketabi\mobile\book_distribution_system`
3. اضغط Run ▶️

## الطريقة 4: VS Code

1. افتح المشروع في VS Code
2. تأكد من تثبيت Flutter extension
3. اضغط F5 أو Run → Start Debugging

---

## بيانات الدخول للاختبار

### مستخدم المدرسة
- **Username:** school_test
- **Password:** school123

### مستخدم مدير المحافظة
- **Username:** province_admin
- **Password:** admin123

### مستخدم مدير الوزارة
- **Username:** ministry_admin
- **Password:** admin123

---

## عنوان الـ API

### للمحاكي (Android Emulator):
```
http://10.0.2.2:8000
```

### للجهاز الحقيقي (Real Device):
```
http://192.168.1.X:8000
```
(استبدل X برقم IP جهاز الكمبيوتر على الشبكة)

---

## خطوات إنشاء طلب من المدرسة

1. **تسجيل الدخول**
   - افتح التطبيق
   - سجل دخول بحساب المدرسة: `school_test` / `school123`

2. **إنشاء طلب جديد**
   - اضغط على "طلب جديد" أو "New Request"
   - اختر الكتب المطلوبة
   - أدخل الكمية لكل كتاب
   - اضغط "إرسال الطلب"

3. **متابعة الطلب**
   - ستظهر حالة الطلب: `قيد المراجعة` (Pending)
   - انتظر موافقة المحافظة

4. **الإشعارات**
   - ستصلك إشعارات عند تغيير حالة الطلب
   - عند الموافقة: `تمت الموافقة على الطلب`
   - عند الشحن: `تم شحن الطلب`
   - عند التسليم: `تم تسليم الطلب`

---

## استكشاف الأخطاء

### إذا لم يتصل بالـ API:
```dart
// تحقق من عنوان API في lib/utils/constants.dart
static const String baseUrl = 'http://10.0.2.2:8000'; // للمحاكي
```

### إذا فشل تسجيل الدخول:
- تأكد من أن Backend يعمل: `docker-compose ps`
- تأكد من وجود المستخدم في قاعدة البيانات

### إذا لم تظهر الكتب:
- تأكد من وجود كتب في قاعدة البيانات
- راجع endpoint: `/api/books/`

---

## ملاحظات مهمة

✅ **تم الانتهاء من**:
- تحديث API Client بدعم JWT
- إنشاء Models متوافقة مع Backend
- تحديث Services لاستخدام Endpoints الصحيحة
- إضافة Auto-refresh للـ Token
- توثيق كامل في INTEGRATION_GUIDE.md

📝 **ملفات مهمة**:
- `lib/services/api_client.dart` - API Client مع JWT
- `lib/services/auth_service.dart` - خدمة المصادقة
- `lib/services/order_service.dart` - خدمة الطلبات
- `lib/models/school_request_model.dart` - نموذج طلب المدرسة
- `lib/models/shipment_model.dart` - نموذج الشحنة

📚 **للمزيد من التفاصيل**:
- راجع `INTEGRATION_GUIDE.md`
- راجع `EXAMPLES.md`
