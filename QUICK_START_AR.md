# 📱 نظام توزيع الكتب - دليل المستخدم السريع

## ✨ ما الجديد؟

تسجيل الدخول الآن يعمل مباشرة من **قاعدة البيانات** 🎉

لا مزيد من كلمات المرور المكتوبة في الكود!

---

## 🎯 الميزات الرئيسية

| الميزة | الوصف |
|--------|-------|
| 🔐 **تسجيل دخول آمن** | استخدام JWT tokens |
| 🗄️ **قاعدة بيانات MySQL** | تخزين آمن للمستخدمين |
| 🚗 **عمليات المندوب** | تتبع الشحنات والتسليمات |
| 🏫 **واجهة المدرسة** | إنشاء وتتبع الطلبات |
| 📱 **تطبيق Flutter** | واجهة سهلة الاستخدام |

---

## 🚀 البدء السريع

### المتطلبات

- Python 3.8+
- MySQL Server
- Flutter (للتطبيق المحمول)

### التثبيت

```bash
# 1. انتقل إلى مجلد المشروع
cd book_distribution_system

# 2. ثبت المكتبات
pip install -r requirements.txt

# 3. شغّل الهجرات
python manage.py migrate

# 4. أنشئ بيانات الاختبار
python manage.py create_test_users

# 5. شغّل الخادم
python manage.py runserver 0.0.0.0:8000
```

---

## 👥 حسابات الاختبار

### مندوب التوزيع 🚗

```
المستخدم: driver1
كلمة المرور: driver123
```

```
المستخدم: driver2
كلمة المرور: driver456
```

### موظف المدرسة 🏫

```
المستخدم: sf1
كلمة المرور: sf1password
```

```
المستخدم: sf2
كلمة المرور: sf2password
```

---

## 🧪 الاختبار

### اختبار بسيط باستخدام Python

```bash
python test_database_login.py
```

### اختبار باستخدام curl

```bash
curl -X POST http://localhost:8000/api/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{"username": "sf1", "password": "sf1password"}'
```

---

## 📊 الاستجابة الناجحة

```json
{
  "access": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "refresh": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "user": {
    "id": 1,
    "username": "sf1",
    "name": "موظف المدرسة 1",
    "role": "school",
    "school_id": 1,
    "school_name": "مدرسة النهضة"
  }
}
```

---

## 🛠️ استكشاف الأخطاء

### ❌ "No module named 'mysqlclient'"

```bash
pip install mysqlclient
```

### ❌ "Connection refused"

```bash
# تأكد من تشغيل MySQL
mysql -u root -p
```

### ❌ "Invalid credentials"

```bash
# تأكد من إنشاء المستخدمين
python manage.py create_test_users
```

---

## 📚 الملفات المهمة

| الملف | الوصف |
|------|-------|
| `book_system/settings.py` | إعدادات Django |
| `distribution/urls.py` | مسارات API |
| `distribution/views.py` | دوال تسجيل الدخول |
| `distribution/models.py` | نماذج قاعدة البيانات |
| `requirements.txt` | المكتبات المطلوبة |

---

## 🔗 روابط مفيدة

- [دليل الإعداد الكامل](docs/FINAL_SETUP_GUIDE.md)
- [دليل المصادقة](docs/DATABASE_LOGIN_GUIDE.md)
- [قائمة المشاكل الشائعة](docs/CHECKLIST.md)

---

## 🤝 هل تحتاج مساعدة؟

1. اقرأ الدليل الكامل في `docs/FINAL_SETUP_GUIDE.md`
2. جرّب `python test_database_login.py`
3. تحقق من السجلات في `output.txt`

---

**آخر تحديث**: 2025
**الحالة**: ✅ جاهز للاستخدام

